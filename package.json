{
  "name": "whizz",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply whizz-db --remote && wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply whizz-db --local"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
